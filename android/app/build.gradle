android {
    compileSdkVersion 33 // Set this to the latest SDK version you want to target

    defaultConfig {
        applicationId "1:286006957836:android:e094f501e0a637d57fdb29"
        minSdkVersion 29  // Minimum SDK version to support Android 10 and above
        targetSdkVersion 33  // Target SDK version
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    buildTypes {
        release {
            // Configure your release build settings here
        }
    }
}
